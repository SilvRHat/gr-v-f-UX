// Multi_verse: RE(Rendering Engine)
// Gavin Zimmerman

// Mv:RE
// Mv:RE allows for the development of multi-verse scenes. The Mv framework has two main parts. 
// These include objects.h and render.h, which allow constructing objects for a scene and
// rendering those objects in a specialized pipeline.
// This framework was developed for a game 'Multi_verse DeliveryService', which features
// portals and the ability of stylizing scenes vastly different. This attributed a custom
// pipeline which then evolved into something robust enough to be used in other projects.

// 

#ifndef MULTI_VERSE_RE_H
#define MULTI_VERSE_RE_H




// DEPENDENCIES
#include "linmath.h"    // Linear Math Library
#include "utils.h"      // Utility Function Library
#include "signal.h"     // Signal Instances
#include "graphics.h"   // Graphics

// PANEL SYSTEMS
#include "objects.h"    // Instance Primitives
#include "render.h"     // Rendering Pipeline


#endif